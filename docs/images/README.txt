Place the two attached screenshots here with these filenames so the MD / HTML
viewers (GitHub, VS Code preview, Confluence after upload) can resolve them:

  rx_counts_plot.png   <- "DTM RX counts (n=43)" matplotlib window
  rx_runner_gui.png    <- "DTM RX Runner" main GUI capture

In Confluence, after attaching the same two files to the page, the
ac:image / ri:attachment macros in `DTM_RX_Runner_Confluence.html`
will pick them up automatically.
